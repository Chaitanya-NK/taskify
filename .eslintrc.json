{
    "extends": ["next/core-web-vitals", "next/typescript"],
    "overrides": [
        {
            "files": ["**/*.ts", "**/*.tsx"],
            "rules": {
                "no-var": "off",
                "@typescript-eslint/no-explicit-any": "off", // Enforce no any usage
                "@typescript-eslint/no-empty-interface": "off" // Allow empty interfaces if needed
            }
        }
    ]
}
